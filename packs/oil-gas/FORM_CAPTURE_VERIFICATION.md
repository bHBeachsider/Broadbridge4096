# Interview Guide form intake verification

24 September 2026. The current request reinstates DOCX intake alongside the
existing JSON-page intake. All code and data stay in Broadbridge4096. No EC2,
model request, PermitHub PIFR/A22 integration, NeonDB connection or push occurred.

## Contract and output

The source is output/expert-capture/Broadbridge_Expert_Interview_Guide_v1.docx.
It was read, not modified. The fixture is a filled copy labeled synthetic on
its first page and in its signatures. It contains no real engineering case,
source rights grant or signed consent.

| Schema | Content |
| --- | --- |
| schemas/case_record.json | broadbridge.case_form/1; identity, B1-B6 decision_time, B7-B13 hindsight, B14 evidence array, questions, sign-off and source hash. |
| schemas/eval_question.json | Part C question type, question, evidence IDs, reference answer, tolerance, hard_fail_criteria array, split and case/family/question IDs. |

Both use JSON Schema Draft 2020-12. Explicit unknown is legal in captured
answers, including enum fields; blanks are null or empty arrays with an issue
path. Objects and arrays keep their declared structure. Metadata such as the
schema version, source hash and approval state is generated by the harness.

The parser emits case_record.json, eval_questions.json and form_review.json.
It uses printed anchors and field labels rather than positional table indexes.
The original page JSON schemas remain unchanged, including their string
hard_fail_criteria; the form version uses an array as requested. The shared
validator and brief runner dispatch by the version marker.

## Synthetic demonstration

```powershell
python packs/oil-gas/scripts/case_from_form.py packs/oil-gas/tests/fixtures/Synthetic_Filled_Interview_Guide.docx tmp/form-demo --family-id FORM-FAMILY-001 --case-signed-off
python packs/oil-gas/run_brief.py tmp/form-demo/case_record.json --dry-run
```

Observed result: FORM-SYN-001, signed, 3 questions, **zero empty boxes**.
Five spare observation rows and three spare hypothesis rows are identified as
unused, not fabricated. Without --case-signed-off the same filled form is
pending_review. Part D signatures alone do not authorize case acceptance.

The direct-file brief dry run contains only identity.unit_service and the six
decision-time fields. Hindsight, reference answers, B14 inventory, consent and
Part A answers do not enter the prompt. The unknown marker is exempt from the
form's retrospective-string check; actual hindsight and reference strings
still trigger a runtime error before transport.

## Tests and review

```powershell
python -m pytest packs/oil-gas/tests -q -p no:cacheprovider --basetemp <fresh-temporary-directory>
```

Final result: **60 passed in 10.04 seconds**: 22 form tests and 38 retained
JSON-capture tests. The suite covers every B item, identity and Part C mapping,
arrays, unknowns, null/flagged blanks, optional spare rows, signatures versus
case acceptance, malformed/duplicate layouts, schema drift, direct CLI parsing,
direct JSON-file briefing and hindsight leakage. Model transport is never used.

Review found that edited signed JSON could bypass the parser's completeness
checks. Six regression cases first failed, then passed after shared validation
was tightened. Signed records now reject blank/null answers, empty required
arrays and unknown identity/family/permission/reviewer values. Explicit unknown
remains valid for engineering knowledge; pending records retain blanks for review.

All eight pages of the synthetic fixture were inspected for legibility. Source
pagination is retained, including spare form space. The updated 11-page plan
was rendered; nine unchanged pages were pixel-identical to the previously
inspected version, and both changed pages were inspected directly.

## Files added or extended

```text
packs/oil-gas/
  schemas/case_record.json
  schemas/eval_question.json
  scripts/case_from_form.py
  scripts/form_contract.py
  scripts/case_contract.py          updated version dispatch
  scripts/run_brief.py              accepts form records
  run_brief.py                      existing pack entry point
  tests/test_form_capture.py
  tests/fixtures/build_form_fixture.py
  tests/fixtures/Synthetic_Filled_Interview_Guide.docx
  requirements-capture.txt          adds python-docx
  README.md
  FORM_CAPTURE_VERIFICATION.md
```

The canonical Markdown/Word infrastructure brief and implementation update
describe the reinstated form workflow. The source guide's ground-rule sentence
mislabels Parts A/B; mapping follows the explicit B1-B6 decision-time items and
B7-B13 hindsight items. Use one case per DOCX. The guide has no family box, so
family membership must be supplied explicitly. No signature authentication,
transcript interpretation, training admission or automatic deployment is claimed.
